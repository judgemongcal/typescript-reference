import { add, sample as samp } from "./utils.js"; // Alias
console.log(add(1, 2));
console.log(samp([23, 56, 31, 53]));
const sample = 3232424;
// Use ES6 instead of CommonJS as Modules in tsconfig to allow your code to run in the browser
